{
  "name": "ember-no-rsvp-codemod",
  "version": "0.1.0",
  "description": "a command line tool for migrating code off rsvp promises",
  "keywords": [
    "codemod-cli",
    "codemod",
    "ember",
    "rsvp"
  ],
  "homepage": "https://github.com/wesleyks/ember-no-rsvp-codemod",
  "bugs": {
    "url": "https://github.com/wesleyks/ember-no-rsvp-codemod/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wesleyks/ember-no-rsvp-codemod.git"
  },
  "license": "MIT",
  "author": "Wesley Sun",
  "scripts": {
    "lint": "eslint --cache .",
    "test": "codemod-cli test",
    "test:coverage": "codemod-cli test --coverage",
    "update-docs": "codemod-cli update-docs",
    "coveralls": "cat ./coverage/lcov.info | node node_modules/.bin/coveralls"
  },
  "bin": "./bin/cli.js",
  "dependencies": {
    "codemod-cli": "^3.2.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "eslint": "^8.1.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^26.6.3",
    "prettier": "^2.4.1"
  },
  "engines": {
    "node": "10.* || 12.* || >= 14"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
